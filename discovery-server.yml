server:
  port: 8761

eureka:
 instance:
    prefer-ip-address: true
  # standalone mode
    client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://discovery-server:8761/eureka/

